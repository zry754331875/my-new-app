import React, { Component } from 'react';
import PropTypes from 'prop-types';
import { connect } from "react-redux";

class EmailInfo extends Component {
    
    componentDidMount(){
        
    }

    render() {
        return (
            <div>1234</div>
        );
    }
}

export default connect()(EmailInfo);